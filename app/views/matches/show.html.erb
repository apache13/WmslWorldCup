<p id="notice">
	<%= notice %>
</p>

<div class="row">
	<div class="col-md-9">
		<%= render "matches/information"%>
		<%= render "matches/result"%>
	</div>
	<div class="col-md-3">
		<%= render "matches/bets" %>
		
	</div>
</div>

<div class="row">
	<div class="col-md-12">
		<% if current_user.admin? %>
		<%= link_to 'Edit', edit_match_path(@match) %> |
		<% end %>
		<%= link_to 'Back', matches_path %>
	</div>
</div>

